package com.hackaaboss.ventaAutos;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class VentaAutosApplication {

	public static void main(String[] args) {

		SpringApplication.run(VentaAutosApplication.class, args);

		/**
		 * Ejercicio Nº 1: Venta de autos

		 * Una concesionaria de automóviles desea realizar un API REST que le permita realizar la carga de
		 * determinados autos usados. Para ello, necesitamos desarrollar los siguientes endpoints:

		 * Dado que el dueño de la concesionaria es un cliente muy exigente, se sugiere realizar buenas prácticas
		 * de programación como por ejemplo la separación del proyecto según la Arquitectura Multicapas.

		 * 💡Tener en cuenta que por el momento, al no estar trabajando con bases de datos, podemos implementar
		 * collections (listas, arraylists, etc) para manejar los CRUD de manera lógica. Recordar que, en base a
		 * lo último aprendido, los datos deben ser cargados desde la capa REPOSITORY
		 **/
	}

}
